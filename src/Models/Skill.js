class Skill {
    constructor(idskill, name, score, displayScore, color, 
                iconPath, skilltext, isEnabled, isIconSkill,
                isSliderSkill, isOtherSkill, isInteresting) 
      {
      this.idskill = idskill;
      this.name = name;
      this.score = score;
      this.displayScore = displayScore;
      this.color = color;
      this.iconPath = iconPath;
      this.skilltext = skilltext;
      this.isEnabled = isEnabled;
      this.isIconSkill = isIconSkill;
      this.isSliderSkill = isSliderSkill;
      this.isOtherSkill = isOtherSkill;
      this.isInteresting = isInteresting;
    }
  }
  
  